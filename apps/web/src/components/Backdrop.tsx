// Calm, static command-center backdrop: a soft brand glow + faint grid. No
// motion — it should recede, not perform.
export function Backdrop() {
  return <div className="backdrop" aria-hidden />;
}
